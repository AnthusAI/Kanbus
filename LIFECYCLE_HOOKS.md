# Lifecycle Hooks

Canonical lifecycle hook documentation now lives at:

- `docs/LIFECYCLE_HOOKS.md`

This mirror is kept for parity with root-level references like `CLI_REFERENCE.md` and `CONFIGURATION.md`.
